#!/usr/bin/env python3

import logging
import sys

import pygit2

from swh.loader.git.git import parse_via_object_list

logging.basicConfig(level=logging.INFO,
                    format='%(asctime)s %(message)s')

parse_via_object_list(pygit2.discover_repository(sys.argv[1]))
