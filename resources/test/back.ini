[main]

# where to store blob on disk
content_storage_dir = /tmp/swh-git-loader/test/content-storage

# Where to store the logs
log_dir = /tmp/swh-git-loader/test/log

# url access to db: dbname=<host> (port=<port> user=<user> pass=<pass>)
db_url = dbname=softwareheritage-dev-test

# activate the compression for each stored object
#storage_compression = true

# compute folder's depth on disk aa/bb/cc/dd
#folder_depth = 4

# Debugger (for dev only)
debug = true

# server port to listen to requests
port = 5001
